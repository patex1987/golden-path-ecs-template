{
  "name": "golden-path-ecs-template",
  "version": "0.1.0",
  "workspaces": [
    "movie-reservation-service",
    "ecs-infra",
    "movie-reservation-web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces --if-present",
    "dev:web": "npm -w movie-reservation-web run dev",
    "check:web": "npm -w movie-reservation-web run check",
    "ci": "npm -w movie-reservation-service run ci && npm -w ecs-infra run ci && npm -w movie-reservation-web run check"
  },
  "devDependencies": {
    "aws-cdk": "^2.1112.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.244.0",
    "constructs": "^10.5.1"
  }
}
